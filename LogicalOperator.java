public class LogicalOperator {

    public static void main(String[] args) {
        // Logical Operator
        // &&

        int a = 60;
        int b = 40;

//        if (a < 50 && b < 50)
//            System.out.println("both less then 50");

        // using " && " the both condition are true, else one are false to output is eals

        // ||
//        if (a < 50 || b < 50)
//            System.out.println("atlast one less then 50");

        // ! Not Operator
        boolean isAdult = true;
        if (!isAdult)
            System.out.println("is Adult");
        else
            System.out.println("Not Adult");
    }
}
